{
  "name": "upgates-com-mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server for Upgates API integration",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "upgates-com-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build",
    "clean": "rm -rf dist",
    "test": "node --import tsx --test src/**/*.test.ts",
    "test:watch": "node --import tsx --test --watch src/**/*.test.ts"
  },
  "keywords": [
    "mcp",
    "api",
    "model-context-protocol"
  ],
  "author": "Lukáš Orčík <OpenMCP - openmcp.cz>",
  "license": "CC-BY-NC-4.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/LukasOrcik/upgates-com-mcp.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "axios": "^1.12.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.20.6",
    "typescript": "^5.7.2"
  }
}
